//macht hier wenig Sinn, kann eigentlich auch in die App.js file direkt rein

import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
  container: {
    flex: 1
  },
});
